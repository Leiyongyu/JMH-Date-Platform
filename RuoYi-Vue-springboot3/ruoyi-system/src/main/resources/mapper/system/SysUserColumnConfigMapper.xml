<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserColumnConfigMapper">

    <resultMap type="com.ruoyi.system.domain.SysUserColumnConfig" id="Result">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="pageKey" column="page_key"/>
        <result property="configJson" column="config_json"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByUserAndPage" resultMap="Result">
        SELECT * FROM sys_user_column_config WHERE user_id = #{userId} AND page_key = #{pageKey}
    </select>

    <insert id="insert" parameterType="com.ruoyi.system.domain.SysUserColumnConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user_column_config (user_id, user_name, page_key, config_json, create_time)
        VALUES (#{userId}, #{userName}, #{pageKey}, #{configJson}, NOW())
    </insert>

    <update id="update" parameterType="com.ruoyi.system.domain.SysUserColumnConfig">
        UPDATE sys_user_column_config SET config_json = #{configJson}, update_time = NOW()
        WHERE user_id = #{userId} AND page_key = #{pageKey}
    </update>

</mapper>
