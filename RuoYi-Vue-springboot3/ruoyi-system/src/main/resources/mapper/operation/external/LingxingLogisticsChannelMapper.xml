<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.operation.external.LingxingLogisticsChannelMapper">

    <delete id="deleteAll">
        DELETE FROM lingxing_logistics_channel
    </delete>

    <insert id="batchInsert">
        INSERT INTO lingxing_logistics_channel (
            id, channel_name, method_id, method_name, billing_type,
            volume_calc_param, zip_code, valid_period, remark, enabled,
            last_modify_uid, gmt_modified, provider_id, provider_name,
            freight_json, send_place_code, receive_country_code,
            is_include_tax, is_points_behind, points_behind_coefficient,
            raw_json, create_time, update_time
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id}, #{item.channelName}, #{item.methodId}, #{item.methodName},
                #{item.billingType}, #{item.volumeCalcParam}, #{item.zipCode},
                #{item.validPeriod}, #{item.remark}, #{item.enabled},
                #{item.lastModifyUid}, #{item.gmtModified}, #{item.providerId},
                #{item.providerName}, #{item.freightJson}, #{item.sendPlaceCode},
                #{item.receiveCountryCode}, #{item.isIncludeTax},
                #{item.isPointsBehind}, #{item.pointsBehindCoefficient},
                #{item.rawJson}, #{item.createTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM lingxing_logistics_channel
    </select>

    <select id="countByIdAndProvider" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM lingxing_logistics_channel
        WHERE id = #{channelId}
          AND provider_id = #{providerId}
    </select>
</mapper>
