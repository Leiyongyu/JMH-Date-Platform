<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.operation.external.EbayPerformanceOwnerRuleMapper">

    <insert id="upsertBatch">
        INSERT INTO ebay_performance_owner_rule (
            stat_month, brand_code, principal_name,
            source_file_name, source_sheet, source_row, imported_by,
            create_time, update_time
        )
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (
                #{row.statMonth}, #{row.brandCode}, #{row.principalName},
                #{row.sourceFileName}, #{row.sourceSheet}, #{row.sourceRow},
                #{row.importedBy}, NOW(), NOW()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            principal_name = VALUES(principal_name),
            source_file_name = VALUES(source_file_name),
            source_sheet = VALUES(source_sheet),
            source_row = VALUES(source_row),
            imported_by = VALUES(imported_by),
            update_time = NOW()
    </insert>

    <select id="selectSummaryByMonth" resultType="java.util.LinkedHashMap">
        SELECT
            stat_month AS statMonth,
            COUNT(*) AS ruleCount,
            MAX(update_time) AS updateTime
        FROM ebay_performance_owner_rule
        WHERE stat_month = #{statMonth}
        GROUP BY stat_month
    </select>
</mapper>
