# 脚本菜单、月度库存、售后数据服务器数据库部署

## 数据库边界

- `date-project`：Python业务数据库，只存放脚本工作台、月度库存、AMZ/eBay售后数据的ODS、DWD、DWS、状态表和Python任务元数据。
- `jmh_data_platform`：Java ERP数据库，只存放ERP菜单权限、Quartz任务，以及领星/谷仓Java同步源表。
- 严禁把两个数据库的表混建。

## 本次涉及的主要表

### 脚本菜单（date-project）

- `image_sop_draft`
- `image_sop_ai_profile_cache`

### 售后数据（date-project）

- AMZ：`ods_amz_sop_sales_daily`、`dwd_amz_sop_sales_daily`、`ods_amz_sop_after_sales`、`dim_amz_sop_after_sales_category`、`dim_amz_sop_classification_cache`、`dwd_amz_sop_after_sales`、`dws_amz_sop_after_sales_summary`
- eBay：`etl_ebay_sop_import_batch`、`ods_ebay_sop_order_raw`、`ods_ebay_sop_after_sales_history`、`ods_ebay_sop_sales_history`、`dwd_ebay_sop_sales_daily`、`dwd_ebay_sop_sales_monthly`、`dwd_ebay_sop_after_sales`、`dws_ebay_sop_after_sales_summary`
- 月份和区间状态：`etl_after_sales_month_state`、`etl_after_sales_range_state`

### 月度库存（date-project）

- ODS：`ods_lingxing_fba_monthly_inventory_detail`、`ods_lingxing_overseas_monthly_inventory_detail`、`ods_lingxing_local_monthly_inventory_detail`、`ods_lingxing_inventory_report_amz_order_profit`、`ods_inventory_report_ebay_sales`、`ods_inventory_report_purchase_order_transit`
- DWD：`dwd_inventory_report_fba_detail`、`dwd_inventory_report_overseas_detail`、`dwd_inventory_report_local_detail`、`dwd_inventory_report_amz_sales_detail`、`dwd_inventory_report_ebay_sales_detail`、`dwd_ebay_inventory_age_cost_snapshot`
- DWS：`dws_inventory_report_dimension_summary`、`dws_inventory_report_department_summary`

### 月度库存Java源表（jmh_data_platform）

- `ods_goodcang_inventory_age_monthly`
- `ods_lingxing_product_procurement_monthly`
- `ods_lingxing_product_supplier_step_price_monthly`
- `ods_lingxing_product_transport_cost_monthly`

## 推荐执行方式

### 1. 先备份两个数据库

至少分别备份：

```bash
mysqldump --single-transaction --routines --triggers -h 127.0.0.1 -u root -p jmh_data_platform > jmh_data_platform_before_20260821.sql
mysqldump --single-transaction --routines --triggers -h 127.0.0.1 -u root -p date-project > date_project_before_20260821.sql
```

### 2. 更新Python数据库

必须进入 `Date-Project` 目录并使用部署环境实际运行Python项目的解释器。项目的 `init-db` 会执行当前 `schema.sql` 和所需迁移，并通过兼容逻辑只补缺失字段，不清空业务数据。

```bash
cd Date-Project
python -m backend.cli init-db
cd ..
```

如果服务器使用虚拟环境，应替换为对应解释器，例如：

```bash
Date-Project/.venv/bin/python -m backend.cli init-db
```

### 3. 更新Java ERP数据库

必须从仓库根目录执行，因为统一SQL通过相对路径调用各模块SQL：

```bash
mysql --default-character-set=utf8mb4 -h 127.0.0.1 -u root -p jmh_data_platform \
  < RuoYi-Vue-springboot3/sql/20260821_three_modules_erp_deploy.sql
```

### 4. 重启并刷新权限

- 重新构建并重启Java后端，使Quartz重新加载最新任务。
- 重启Date-Project Python服务。
- `leiyongyu`退出ERP后重新登录，刷新菜单与按钮权限缓存。
- 不需要清空任何业务表，也不要执行AMZ售后测试用的重置SQL。

## 关键调度口径

- AMZ售后：每周日22:30刷新当前月；进入新月份后的第一次任务强制重拉上一个完整自然月。
- 月度库存三表源数据：每月1日拉取上一个完整自然月。
- 月度库存实际达成及销量：每月11日23:00拉取上一个完整自然月。
- 次月月初库存：使用上月海外仓/FBA期末库存数量回填。

## 交给服务器AI的执行提示词

```text
你现在位于已经git pull最新代码的 JMH-Date-Platform 仓库。请完成脚本菜单、月度库存数据表、售后数据三个模块的数据库升级和部署验证。

严格要求：
1. 先定位仓库绝对路径、Python实际解释器、Java启动方式、MySQL连接配置；不要猜路径和密码。
2. jmh_data_platform是Java ERP库；date-project是Python业务库，绝对不能混用。
3. 在修改前分别使用mysqldump备份两个数据库，并确认备份文件非空。
4. 不得执行DROP DATABASE、TRUNCATE、全表DELETE、20260807_amz_sop_reset_test_tables.sql，也不得清空现有业务数据。
5. 进入Date-Project目录，使用该服务实际Python解释器执行：python -m backend.cli init-db。若失败，停止后续步骤并报告完整错误。
6. 回到仓库根目录，使用mysql客户端执行：RuoYi-Vue-springboot3/sql/20260821_three_modules_erp_deploy.sql。该文件包含SOURCE相对路径，所以必须从仓库根目录执行。
7. SQL全部成功后，重新构建并重启Java后端，再重启Date-Project Python服务。
8. 检查Java、Python进程和端口；检查Java启动日志没有Bean、SQL、Quartz异常；检查Python健康接口返回成功。
9. 查询sys_job确认：AMZ-SOP售后链路启用；月度库存源数据、实际达成及销量、次月月初库存任务启用；实际达成及销量Cron为0 0 23 11 * ?。
10. 查询sys_menu和sys_role_menu确认leiyongyu具有：sop:scriptTools:view、sop:imageSop:use、finance:monthlyInventoryReport:list、finance:monthlyInventoryReport:edit、sop:afterSales:list、sop:afterSales:sync、sop:afterSales:export、sop:afterSales:import。
11. 最后只做只读验证，不要主动运行会拉取或覆盖业务数据的Quartz任务。

最终向我报告：两个备份文件路径及大小、init-db结果、ERP SQL执行结果、重启结果、任务Cron核对结果、leiyongyu权限核对结果，以及任何失败的原始错误。
```
