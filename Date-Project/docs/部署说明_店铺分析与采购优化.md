# 部署说明：店铺分析与采购优化

更新：2026-09-09。先核对环境和备份，再执行。本轮只涉及06、07，不执行同目录01～05或99；尤其不顺带执行其他授权脚本。

## 一、本轮交付

1. 补货2.0产品等级规则编辑器（9条）。
2. SKU分析成都/海外在途、可售4列。
3. 经用户明确授权的待采购删除功能（status=0/1均可，选中记录、确认后删除）。
4. 安全库存2、建议补货量2，共用原系数，输入改为未取整预估销量2/30。

目标数据库是 **jmh_data_platform**。本轮没有在 Date-Project 库建表，不修改历史订单、利润、库存或采购数据。

| 部署文件 | 用途 |
| --- | --- |
| 06_店铺分析与采购优化.sql | CREATE TABLE IF NOT EXISTS等级规则表；只补缺失规则和remove菜单；不覆盖已有配置、不自动授权 |
| 07_店铺分析与采购优化_验证_只读.sql | 9项检查（20条USE/SELECT语句），不写数据 |
| 仓库sql/20260909_shop_analysis_procurement.sql | 06的同语句仓库副本 |
| 仓库sql/20260909_shop_analysis_procurement_verify.sql | 07的同语句仓库副本 |

“仓库sql”指 `RuoYi-Vue-springboot3/sql`。部署只执行一套即可，不需两份都跑。

## 二、本机实际执行记录

执行时间：2026-09-09 15:54:34，目标 `127.0.0.1:3306/jmh_data_platform`。

- 06执行两次，07执行一次，均完成。
- 执行前规则表已有9条，执行后仍9条；新增规则0条，原条件、等级、说明、状态、修改人和时间均未改变。
- 删除菜单已存在，本次新增菜单0条；角色授权未改变。
- 采购表执行前后均1条，全部字段对比一致；**没有执行任何采购DELETE**。
- 07规则完整性四项OK；7个库存仓共4449行；检查9缺失项返回0行。
- 重复执行前后配置、菜单、授权、采购记录及相关表结构一致。

06执行文件SHA256：
`cf1cd81a47d0ed8bffbbef8b30ee7cae651f8bcbcb5d60995053100c30385467`

备份/审计目录：

`D:\JMH\项目\部署sql\backups\shop_analysis_20260909_155434_265410\`

包含 before.json、after.json、executed_06.json、repeated_06.json、verification_07.json、result.json。保存了相关表结构、等级配置、待采购菜单/授权及采购记录，目录为新建且文件不覆盖。这是本轮定向备份，**不是整个系统的灾备替代品**，不要把备份提交到Git。

执行器 `Date-Project/scripts/apply_shop_analysis_upgrade.py` 仅允许本机数据库；检查文件哈希和语句白名单，异常即停止并rollback未提交事务。没有启动应用或外部同步。生产部署不能直接复用本机备份，必须另行备份生产库。

## 三、部署机步骤

### 1. 先检查、备份

确认连接的是部署机正确的jmh_data_platform库；检查现有等级规则是否有人工配置、待采购父菜单是否唯一、权限和表结构是否完整。旧规则不应通过重跑SQL重置。

使用你们现有的、已验证可恢复的数据库备份流程，创建新的带时间戳备份；不要覆盖旧备份。至少保留等级规则、sys_menu/sys_role_menu、采购表及结构；推荐完整库备份并验证恢复可用。

### 2. 执行06，再执行07

在MySQL客户端中执行（替换为部署机实际路径）：

```sql
SOURCE D:/JMH/项目/部署sql/06_店铺分析与采购优化.sql;
SOURCE D:/JMH/项目/部署sql/07_店铺分析与采购优化_验证_只读.sql;
```

SOURCE属于MySQL客户端命令，不是PowerShell命令。

**遇到任何SQL错误立即停止后续步骤，检查错误和事务状态，不以最后一行“完成”当成功证据。** DDL可能隐式提交，不能靠一个rollback撤销所有DDL。06设置15秒锁等待；业务繁忙时仍可能等待元数据锁，因此不能宣称“不锁业务表、不需要维护窗口”。建议低峰执行。

初始配置期望9条、序号1～9、等级合法；人工停用某条规则后status=0是合法配置。没有恒真兜底时，07会提示无匹配可能显示--，应由业务确认，不能擅自恢复默认值。

07菜单查询包含list页和add/export/remove三个按钮，完整时是4行，不是3行。若父菜单缺失，先部署采购中心；若父菜单重复，先人工排查，不按开发机menu_id盲插。

### 3. 发布代码

- 构建并发布ERP前端新的dist，避免仍使用旧静态页面。
- 重新构建Java并替换部署机实际使用的JAR，再按你们服务流程重启Java；不能只重启旧JAR。
- 发布Python代码，按运行方式重启或重新加载。生产不应假定启用了uvicorn reload。
- 本轮本机未自动重启、未替换运行JAR、未推送Git。

### 4. 权限与页面

- 等级规则复用 `operations:ebayReplenishmentV2:formula`。有权限时点击产品等级**表头**进入；无权限不显示编辑按钮，Java接口仍校验权限。
- 删除使用 `procurement:pendingPurchase:remove`。SQL只建菜单，不给角色授权；由管理员按需勾选，账号重新登录后验证。
- 第二组安全库存/建议补货量共用原系数弹窗；提示明确影响全部同级别SKU和两组数值。
- 若保存过旧列配置看不到新增列，先到列设置勾选，不要清空整个系统或删除列配置表。

## 四、已完成验证

- Python离线回归：138 passed、1 skipped（可选部署检查）。
- 初始等级规则与旧硬编码在21³=9261组输入上等价，包括全部阈值附近和None。
- Java编译及5项Mockito采购删除测试通过；前端生产构建通过。
- 强制只读真实数据比对：补货2.0首200行原字段差异0，总数1704；SKU分析首200行原字段、图表和汇总不变；四项库存与补货2.0一致，切换有订单的单日范围库存不变。
- 第二组取整、缺数据返回None和共享系数已覆盖离线测试。

尚需部署环境验收：

- 使用有权限/无权限账号验证规则接口、删除接口的权限拒绝。
- 编辑规则、保存冲突、页面新列和时效失焦后的四字段更新。
- 用**隔离测试记录**验证单选/多选及混合状态删除、已采购禁止重复导出、删除与导出并发。禁止直接拿真实采购记录做“试删”。
- 模拟单测不等于生产并发验证；本轮没有实际删除采购记录。

## 五、回滚原则

优先回滚应用代码并保留新增表、配置和菜单数据，不执行DROP TABLE。新代码缺规则表时等级及相关补货值会显示--，删表并不是安全回滚。

若要关闭删除能力，应同时回退/停用接口、撤销相关权限并刷新登录会话；只隐藏菜单不是完整的后端防线。业务数据恢复必须使用经过验证的备份，并另行审批，不能因回滚代码而清空整表。

库存4列和第二组补货是查询期派生，不需要回滚业务数据。
## 2026-09-09后续修正：已采购也能选中删除

按用户最新确认，待采购和已采购均可勾选并删除，删除前明确提示其中已采购条数及历史记录不可恢复。混合勾选时，导出按钮只导出待采购，显示对应数量；后端仍拒绝已采购重复导出。

删除按钮不再因权限不足而消失：无权限时置灰，显示所需权限及重新登录提示。后端继续校验 `procurement:pendingPurchase:remove`，本轮没有自动给账号或角色授权。

本修正只改前后端代码，不需要执行新增SQL，也没有实际删除数据。此前06/07中的“只删除未导出记录”等历史备注不再描述当前行为；已执行文件和哈希保留供审计，实际限制以当前后端Mapper为准。发布新前端，并部署新JAR、重启Java后生效。

## 2026-09-09 16:23：leiyongyu 全部现有权限授权（已执行本地）

本节是用户随后明确授权的独立操作，覆盖前文“未自动授权”的历史状态；不是06脚本自动附带的授权。

- 执行文件：[08_授权leiyongyu全部现有权限.sql](08_授权leiyongyu全部现有权限.sql)，目标库 `jmh_data_platform`。
- 仓库副本：`RuoYi-Vue-springboot3/sql/20260909_grant_leiyongyu_all_permissions.sql`。
- 05脚本仅补周报的两项权限，不是全权限脚本。本次全权限需求使用08，不改写05的历史记录。
- 原角色101由admin与leiyongyu共享，因此没有给该共享角色加权限。新建专用角色 `leiyongyu_all_permissions`，只关联leiyongyu；数据范围为1（全部数据）。
- 本地执行时间：2026-09-09 16:23:13；地址127.0.0.1:3306。本地账号ID101、新角色ID103仅作记录，SQL按账号名和role_key查找，不写死这些ID。
- 实际新增：1个角色、1个用户角色关联、188个角色菜单关联；对应165个有效非空权限标识，遗漏菜单0，包含 `procurement:pendingPurchase:remove`。
- 同一SQL执行两次，第二次无数据变化；原角色、其他账号关联及菜单定义完全不变。没有修改密码、创建数据库用户、执行MySQL GRANT、删除采购记录或触发同步任务。

### 备份和审计

备份目录：`D:\JMH\项目\部署sql\backups\leiyongyu_permissions_20260909_162313_634148`。含before.json、after.json、run_1.json、run_2.json、result.json，记录权限数据和每条SQL结果；不包含账号密码。

执行文件及仓库副本SHA256均为：
`843216d3461f25e7e58ad838c89b4f6626b4a8c284aea5ef07c8d22f1ba568b0`。

### 部署机操作及边界

本次只在本地执行，**不代表部署机已授权**。部署机先备份sys_role、sys_user_role、sys_role_menu，再用MySQL客户端执行08；必须检查输出grant_status=GRANTED、missing_menus=0。若为REFUSED，检查账号是否唯一且启用、专用角色是否被其他账号占用或停用，不可绕过保护强行执行。

授权只覆盖执行时存在的菜单/按钮，不启用被停用的菜单，也不赋予user_id=1的代码内置超级管理员身份。以后新增菜单需要重新执行08补齐。不要把此专用角色分配给其他人；发现共享时脚本会拒绝写入。

**leiyongyu必须退出系统重新登录**，刷新缓存中的权限；仅刷新页面不够。授权SQL本身无需重启服务，但此前的删除按钮、已采购可选择等代码修正仍需部署新前端和新Java包。数据库授权验证通过，不等同于已完成浏览器点击或接口权限验收。

如需撤销，应先核对专用角色仅绑定leiyongyu，再由管理员撤销这条用户角色关联并刷新会话；保留原有角色和业务数据，不删除共享角色。

## 2026-09-09：补货2.0人工销售类型（代码完成，本地09与08 SQL已执行）

### 功能口径

在“产品性质”后新增独立的“销售类型”：正常 / 刷单。新品、老品仍按原规则自动判断，二者不互相覆盖。未设置标记的SKU在表已部署时默认为正常；按站点和完整SKU保存，不去前后缀，不按月份清空。刷新、切月份、订单重传不会覆盖标记，同一键再次保存更新原行。

销售类型与站点、SKU、产品等级、产品性质筛选可以叠加；销售类型条件在SQL分页之前应用，越界页计数同样带条件。标记当前仅用于显示和筛选，不剔除销量，不修改预测、退货率、产品等级和补货公式；不同销售类型的计算公式待业务另行确定。

### 部署步骤（与先前08授权记录分开）

1. 备份数据库权限配置。执行 `D:\JMH\项目\部署sql\09_补货2.0销售类型.sql`，目标库为 `jmh_data_platform`。仓库副本为 `RuoYi-Vue-springboot3/sql/20260909_ebay_replenishment_v2_sales_type.sql`。
2. 新建 `ebay_replenishment_v2_sales_type`，主键(site,sku)，类型NORMAL/BRUSH，记录最后修改账号和时间。SQL只CREATE IF NOT EXISTS和补缺失菜单，不插入默认业务行，不覆盖已有人工标记，不删除历史数据。空表正常。
3. 新权限 `operations:ebayReplenishmentV2:editSalesType` 挂在补货2.0页面下。SQL不自动授权角色。角色管理勾选新权限；如仍按此前leiyongyu全部现有权限要求授权，可另执行08脚本补齐，然后退出重新登录。此前08执行时尚无本权限，不能认为已经具备。
4. 部署新Java JAR并重启Java，更新Python代码及前端构建产物。接口为Java `POST /operations/ebay/replenishment-v2/sales-type` → Python内部同名sales-type端点，列表新增salesType/sales_type参数。
5. 有编辑权限显示行内下拉，选择后自动保存；无编辑权限仅查看。保存失败保留旧值。若有旧自定义列配置，可在“自定义列”中勾选销售类型并拖到产品性质后，不清空整份列配置。

缺表时原列表仍能加载，但销售类型显示--、不可编辑且有部署提示；不会把丢失的配置假装成正常。带销售类型筛选但缺表会明确拒绝。任意数据库错误不统一吞掉。新增接口的Java权限校验不可由前端隐藏替代，操作人始终由登录会话覆盖，不信任请求里的operator。

### 验证范围

已完成离线Python回归、前端保存成功/失败/无权限检查和Vue编译；Java新增接口权限注解及操作人覆盖测试通过，Java打包通过。未启动或重启服务，未修改真实人工标记，未进行浏览器登录验收。

### 本地SQL执行记录（2026-09-09 17:19:54）

- 已按顺序执行本目录09_补货2.0销售类型.sql、08_授权leiyongyu全部现有权限.sql；目标仅为本机jmh_data_platform，不代表部署机已执行。
- 新建ebay_replenishment_v2_sales_type空表；新增修改销售类型菜单（本地menu_id=2164，父菜单2152）。部署时仍按perms定位，不可照抄ID。
- 给leiyongyu（user_id=101）的独占角色103新增1条菜单授权；现有189个菜单全部授权，缺失数0。新权限为operations:ebayReplenishmentV2:editSalesType。
- 两份脚本均再次执行验证，第二轮无数据变化；原角色、用户绑定、其他角色授权及已有菜单保持不变，人工标记仍为0行。
- 执行前备份及两轮结果：D:\JMH\项目\部署sql\backups\sales_type_permissions_20260909_171953_910851（before.json、after.json、run1.json、run2.json、result.json）。
- 脚本SHA256：09为e01e0db12b316a4b0bd6d4f8aef97c82a29a203cc36cb3e929b661ad3d786c63；08为843216d3461f25e7e58ad838c89b4f6626b4a8c284aea5ef07c8d22f1ba568b0。
- 本地测试前退出账号并重新登录以刷新权限；若仍运行旧程序，请自行加载新Java/Python及前端版本。本次未替用户重启。

部署后核验：未设置显示正常；同SKU不同站点独立；可正常↔刷单反复修改；刷新/重传订单后标记保留；与新品和S等级组合筛选分页无重复且total正确；无权限直接调用保存接口被拒绝。数据库保存采用事务按键upsert，多人同改时以后提交的值为准，修改人和时间可追溯。
