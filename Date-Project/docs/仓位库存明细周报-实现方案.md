# 仓位库存明细周报：实现与部署说明

### 页面刷新会话修复（2026-09-10，无新增SQL）

- 原因：首次打开后页面移除了URL中的erp_session，但Java代理只读取URL凭证，刷新时在HTML加载前即被拒绝。
- 首次从脚本中心打开并通过权限校验后，Java签发周报专用HttpOnly、SameSite=Strict会话Cookie；HTTPS时加Secure。Cookie由浏览器按外部代理目录限定路径，兼容/prod-api前缀，不设置Domain。
- 页面继续清理地址栏凭证；列表、生成和下载不再把会话写进URL，使用同源Cookie。每次请求仍校验原Redis会话及sop:weeklyInventory:use，未延长会话有效期。Cookie认证的生成请求要求页面专用请求头，并拒绝跨站请求。
- 部署须同时更新并重启Java（新JAR），更新Date-Project/frontend/public/weekly-inventory/index.html及前端构建产物；仅重启Python不能加载Java修复。部署后从脚本中心重新打开一次以建立Cookie，再刷新验证。真正过期或无权限时仍需重新打开或处理授权。
- 无数据库、权限菜单或定时任务变更；不重拉数据、不覆盖历史文件。8项Java代理测试与5项页面测试通过，尚未做部署机浏览器实测。

### 页面生成与定时拉取分离（2026-09-10，无新增SQL）

- 页面按钮改为“使用现有数据生成 Excel”：原POST /api/v1/weekly-inventory/run只提交已有快照生成，不再调用领星。Java代理路径和原有sop:weeklyInventory:use权限不变。
- 定时任务pythonWeeklyInventoryTask.runWeekly()仍执行拉取→完整性校验→ODS事务写入→自动生成Excel，原cron不变。
- 复用最新成功批次，以库存ODS的pulled_at排序，不以文件生成时间排序；快照日期保留原拉取日期。无成功快照时页面禁用、接口400，不偷偷触发拉取。拉取完成但文件失败的批次暂不自动选取，避免把失败批次当作成功数据。
- 不新建ODS批次、不复制库存明细。重新生成时sync_batch_id保留源批次，文件名使用独立UUID，trigger_type=manual_snapshot；历史文件保留。文件完成/失败更新按export_code+sync_batch_id+file_name定位，不能再按批次更新全部文件。
- 页面展示可用快照日期与拉取时间，并区分“已有快照重新生成”与“拉取后生成”。异步失败仍写任务日志，文件列表也回显失败提示。
- 两条链路共用inventory:weekly-export数据库锁，避免拉取、生成及中断清理互相干扰。试图同时生成时应稍后重试。
- 无数据库结构、权限或Java改动。更新Python及frontend/public/weekly-inventory/index.html（前端构建已完成），重启Python后重新打开脚本页面；不要仅刷新仍使用旧Python的页面。

验证：116项后端离线测试、5项页面测试通过，前端构建通过。只读检查本地批次9e121669-d08a-4b55-ab56-f6208b29217b（2026-09-10 10:40:32）：库存8577、库龄34308、仓位26336、产品4355条，构建8539行29列，无第三方导出列。本轮未调用外部接口、未写业务数据或生成真实留底登记、未重启服务；实际页面生成留待加载新代码后验收。


更新：2026-09-09。本文件描述当前代码，替代此前含日期覆盖、独立MAX聚合及无鉴权直连的草案。

## 1. 功能与边界

脚本中心“仓位库存明细周报”：历史文件分页、后台使用已有成功快照生成新Excel、下载成功文件。每周一07:30由Java Quartz拉取并自动生成新批次。页面不调用领星、不重复写ODS；全仓库存、仓位、产品数据由定时链路落入4张ODS表，文件登记在`ops_weekly_export_file`。

不修改月度库存、绩效、滞销清货或eBay价格查询的计算。历史Excel和ODS批次没有自动清理逻辑；需备份数据库及导出目录、监控磁盘空间。

## 2. 已修正的设计问题

| 原方案问题 | 当前实现 |
| --- | --- |
| 按日期删除再插入，旧文件无法对应旧ODS | 唯一键加入sync_batch_id；4表单事务追加；查询固定批次，不按当天拼接 |
| 同仓同产品库存逐字段MAX、库龄跨seller求和 | 按product_total最大、seller_id字典序打破并列，选完整代表行；库龄、第三方库存跟随同一seller，避免虚构组合或重复求和 |
| 静态路径未挂载、接口没有权限保护 | Java独立代理逐次校验ERP会话与`sop:weeklyInventory:use`；Python接口校验内部令牌；浏览器无内部Token |
| 只按日期或8位批次命名 | 完整UUID文件名；同盘临时文件完成后排他发布，已有文件绝不覆盖 |
| 缺失值补0、固定38列 | 缺失保留空白，测得0保留0；25个固定列+实际库龄分档列（2026-09-10按用户要求移除第三方9列） |
| 宣称纯建表、无ALTER | 迁移包含兼容补product_name、扩model和升级4个唯一索引；不删除业务数据 |
| openMode注册无效 | 使用现有transport/proxyBase/needSession契约，新标签页打开 |

代表行规则是确定性的库存去重口径，不代表接口三次请求是一个数据库时间点。领星源数据可能在拉取期间变化；接口total变化、分页短缺、重复主键、产品详情缺失会拒绝本批次，而非生成表面成功的不完整报表。

## 3. 抽取与原值留存

复用`LingXingClient.post_signed_query_auth`，不添加独立密钥或自行签名。

| 接口（共同前缀erp/sc/routing/data/local_inventory/） | 请求 |
| --- | --- |
| inventoryDetails | offset/length，800条/页，不传wid过滤 |
| inventoryBinDetails | offset/length，500条/页，不传wid过滤 |
| batchGetProductInfo | 库存中的product_id去重，每批最多100个productIds |

分页检查code、data、total及页数；产品详情返回id集合必须等于请求集合。接口空字符串转NULL，数值使用Decimal，主键必须是有效整数。三个接口整行保存raw_json，常用列平铺；新增未识别字段仍保留在raw_json。

映射：产品详情id→product_id；仓位lockNum→lock_num、validNum→valid_num；store_id、msku、product_name保留；whb_type不按旧文档限制枚举。model保持VARCHAR(1000)。字段尺寸超出数据库容量时事务回滚，不截断原值。

表及当前列数：库存34、库龄10、仓位23、产品39、文件登记14。仓位新增1个数据库生成辅助键，16个接口原字段仍保留。四个ODS唯一键均包含snapshot_date,sync_batch_id；仓位完整业务键包含仓库、仓位、产品、店铺、MSKU、FNSKU。首次部署和已建过表环境都需执行20260910仓位键迁移，详见第11节。

## 4. Excel口径

输出一行一个(wid,product_id)，稳定按仓名、SKU、主键排序。代表行相同物理产品出现不一致SKU时拒绝导出。仓位先按(wid,product_id)汇总，避免多仓位导致行数膨胀。仓库名来自配置库`SHOP_SOURCE_DATABASE`的warehouse表，仓位wh_name作为缺名时回退；仍缺失则显式失败。

列顺序：

1. 仓库名称、SKU、产品名称、本地产品id。
2. 实际库存总量、可用量、次品量、待检待上架量、锁定量。
3. 库龄竖表实际分档，按bucket_index、名称稳定排序。只取代表seller的qty，不跨seller累加。
4. 锁定量(仓位)、未锁定量(仓位)。缺仓位或任一参与值未知时不伪造总量。
5. 采购产品/包装/外箱长宽高(CM)，产品净重(G)，产品毛重(KG)，外箱实重(KG)。产品毛重在导出时将源字段cg_product_gross_weight（克）除以1000；ODS及raw_json原值不变，空值仍为空、0仍为0，净重和外箱重量不换算。
6. 采购单价purchase_price；库存金额=purchase_price×product_total，任一缺失则空白，不使用产品档案cg_price替代。

2026-09-10导出优化：第三方可用量/调拨在途/锁定量的系统、三方仓、差异共9列不再导出（即使源值非空也不输出），但ODS与raw_json原始数据继续保存。4个库龄分档时新文件为29列；库龄分档仍按实际接口动态生成。整数用数字格式`0`，非整数用`0.0#####`，消除原`0.######`造成的整数末尾小数点；小数最多显示6位，与原精度一致，底层数值、金额计算及空白含义不变。

本项仅更新Python导出代码，无需SQL或Java/前端改动。加载新Python代码后新生成的文件生效，历史下载仍返回原留底文件，不会自动覆盖或改格式。本轮未触发真实数据拉取。

openpyxl流式写入，所有源字符串强制作为文本，防止以“=”开头的SKU或产品名变成Excel公式。金额计算不降级float；Excel数值本身受其有效位数限制。

## 5. 文件、失败与恢复

目录：`{EXPORT_OUTPUT_DIR}/weekly_inventory`，默认是Date-Project/exports下。文件：`仓位库存明细_YYYY-MM-DD_<完整UUID>.xlsx`。

定时链路状态：RUNNING→SUCCESS或FAILED。获取全局任务锁后建立RUNNING；源完整校验后4ODS表一次提交，再从该批次读回生成Excel；同盘临时文件刷盘后用硬链接排他发布，最后登记SUCCESS。页面生成则跳过拉取与ODS写入，直接读取最近一次成功快照。目标卷需支持硬链接（部署Windows应使用NTFS），不支持则明确失败，不能退化为覆盖旧文件。

文件发布后登记失败：文件和ODS保留，不伪报成功；按批次日志核对后人工恢复登记或重跑产生新批次。进程崩溃遗留RUNNING，在下一次取得全局锁时标为INTERRUPTED，旧文件及数据不删除。未提交阶段错误不会写半份ODS。

全局MySQL命名锁`inventory:weekly-export`覆盖整条链，跨日期、跨worker、手动与Quartz互斥。页面后台命令提交不等于完成；真实状态以文件登记和scheduler_task_run为准。若进程在排队但尚未开始记录任务时退出，需要重新提交。

下载仅接受文件登记ID；必须SUCCESS、路径位于导出根目录、文件名一致且文件存在，禁止客户端传任意服务器路径。

## 6. 接口与权限

Python内部前缀`/api/v1/weekly-inventory`：GET /page、GET /files?page=1&limit=20、GET /files/{id}/download、POST /run。均校验require_internal_access。

Java入口`/sop/weekly-inventory/proxy`：GET /index.html、GET /files、GET /files/{id}/download、POST /run。逐请求校验`sop:weeklyInventory:use`及ERP临时会话；方法与路径白名单；不跟随上游重定向；从原始query读取会话，避免getParameter消费表单正文。只使用既有jmh.python-performance服务地址和内部Token，不新增密钥。Python8010不要暴露公网，配置双方一致的内部Token。

UI请求同源代理目录，不能用任意api_base把ERP会话发到第三方。首次打开用临时erp_session换取周报专用HttpOnly Cookie；后续列表、生成、下载及整页刷新都用Cookie继续校验，不把内部Token放进页面。页面移除地址栏会话参数并设no-referrer；访问日志仍应脱敏首次打开时的erp_session查询参数。

注册表transport='proxy'、needSession=true；ERP脚本菜单父权限`sop:scriptTools:view`，子F按钮`sop:weeklyInventory:use`。部署后给目标角色勾选，不能只靠隐藏卡片做鉴权。

## 7. 调度

- Quartz名称：仓位库存明细周报。
- 调用：`pythonWeeklyInventoryTask.runWeekly()`。
- Cron：`0 30 7 ? * MON`；Java与Python服务器时区须为Asia/Shanghai。
- Python任务编码：`weekly_inventory_bin_export`。
- 内部执行：POST `/api/v1/internal/scheduler/tasks/weekly_inventory_bin_export/run`，空JSON即可；禁止传stat_month/start_date/end_date，实时数据不可贴历史标签。
- Python scheduler_task仅登记、默认enabled=0，由Java负责调度，防止双调度。手动执行仍可用。
- Python写scheduler_task_run并更新last_run_at；Java写operation同步日志与Quartz结果。
- 复用Python绩效任务的长读超时（默认3小时）；部署若自定义了短超时，应调大，否则Java可能先超时而Python继续执行。此时不要反复启动任务，先查RUNNING及日志。

## 8. 部署文件与顺序

部署目录`D:/JMH/项目/部署sql`保留第一轮01_/99_，本轮使用：

1. `02_仓位库存明细周报建表.sql`（date-project）。已建旧版也必须重跑，升级批次唯一键。
2. `04_仓位库存明细周报_菜单与定时任务.sql`（两个库各自USE、各自事务；不覆盖已有人工cron/status，不自动授权所有角色）。
3. 执行 `10_周报仓位业务键修复.sql` 后执行 `11_周报仓位业务键_验证_只读.sql`，详见第11节。
4. `03_仓位库存明细周报_验证_只读.sql`，检查表、索引、任务、菜单及生成后的批次。

仓库内对应：Date-Project/migrations/20260909_weekly_inventory.sql、20260909_weekly_inventory_verify.sql、RuoYi-Vue-springboot3/sql/20260909_weekly_inventory_job_menu.sql。schema.sql同步新建表结构；仅更新schema不能升级已存在的日期唯一键，必须执行迁移。

部署前备份两个库及导出目录，停用此新任务后执行迁移；建索引可能等待元数据锁，应安排维护窗口。构建部署Date-Project/frontend/dist和Java JAR，重启Python与Java；角色授权后从ERP重新进入脚本中心；再手动生成一次进行真实验收。01_/99_是否执行由部署者按上一轮说明核对，本轮不删除、不代执行。

## 9. 验收与当前验证范围

离线测试覆盖：完整/缺失分页、重复主键、Decimal、库存代表行与库龄一致性、仓位聚合、空值和零、动态列、Excel公式注入、不可覆盖文件、路径越界、事务回滚、内部鉴权、历史日期拒绝。Java独立测试验证缺失/伪造会话403、路径和方法白名单、query解析。

部署后仍需验证：全仓真实接口字段、成功文件每格对照、同日重跑两份文件各自对应ODS批次、权限账号真实登录、定时任务日志、断网/磁盘异常与长任务状态。此前用户提供的两仓1831行冒烟统计属于旧方案实验记录，不能替代这次实现的真实验收；本轮不调用领星、不执行数据库脚本、不启动服务。

## 10. 2026-09-10：接口诊断与有限重试

背景：本地09:13周报在inventoryBinDetails响应校验处失败，旧日志未保留原始code/message，不能确定为限流。随后只读诊断按500条分页检查53页、26334条全部成功，不代表原故障已修复，也不是完整周报生成验收。

### 重试边界

- 仅周报的inventoryDetails、inventoryBinDetails、batchGetProductInfo三个只读接口使用新重试层。每一页/产品批次最多4次请求（首次+3次重试），始终使用同一offset或productIds；成功才推进分页。
- 明确业务限流码3001008；HTTP 408/429/500/502/503/504；超时、连接重置/中断、断管及暂时DNS解析失败可以重试。未知业务码、鉴权/参数错误、永久DNS失败、JSON或data/行结构异常不自动重试，不凭错误消息的“busy”等字样猜测。
- 等待2、4、8秒，加0～0.5秒抖动。识别HTTP Retry-After秒数/日期，等待不少于该值；若要求等待超过60秒则本次显式失败，不提前再次请求。次数耗尽明确报错，不静默跳页。
- 周报创建LingXingClient(max_retries=0)，避免底层和周报两层重试相乘。共享客户端仅新增兼容ValueError的结构化HTTP异常元数据，其他任务的重试配置和成功响应处理不变。

### 日志和错误记录

- Python日志：logs/date-project-api.log，成功页记录接口/批次/页码/行数；重试记录attempt和等待秒数；失败记录接口、batch、page、offset、length、产品批次数量、业务码或HTTP状态、data类型、上游request_id及脱敏message。
- 页码page从1开始；产品详情的page表示100个产品一组的批次序号，不输出实际产品ID清单。日志上下文仍携带调度requestId。
- 只提取错误码、message/msg和request_id，不输出完整响应、请求体、签名URL、data或error_details。隐藏已知凭证以及带敏感标签的Token/Secret/签名等，移除控制字符并限制消息长度。最终异常不携带原始网络异常链，防止LOG.exception将签名地址再次打出来。
- 安全摘要随异常传给Python接口、scheduler_task_run.error_message、Java任务告警；ops_weekly_export_file.error_message同时保留本次批次和安全摘要。复用现有TEXT字段，不新增日志表。

### 完整性和部署

total缺失/变化、超量/提前结束、重复主键、产品缺失/重复/非请求产品等校验保留；这些失败不重试、不补0、不跳过。必须三个源全部成功且校验通过才调用insert_snapshot；失败保留已有快照和历史文件。

本次无需SQL、无需改Java/前端。更新Python代码后按运行方式重启或重新加载；生产不要假定开启reload。旧失败日志无法补出当时未记录的原始响应，新诊断信息从更新后生效。本轮实现验证使用模拟接口/数据库，不再次调用领星、不写真实快照、不自动重启服务。

## 11. 2026-09-10：仓位明细完整业务键修复（本地已执行，部署机待执行）

### 根因与数据口径

本地09:47任务的三个源接口均成功，随后Python把同仓同仓位同产品的不同店铺行误判为重复。实际同页记录：
- wid=18678、whb_id=59911、product_id=403591，store_id=0，总量10、锁定10。
- 同一三字段，store_id=12633，总量0、锁定0。
旧Python校验和uk_bin_week索引均漏掉店铺维度，不能删除“重复行”绕过。

现在按(wid,whb_id,product_id,store_id,msku,fnsku)校验；保留各明细及raw_json，导出仍按(wid,product_id)汇总仓位锁定/未锁定量。同一完整业务键重复（即使数量完全相同）仍拒绝，不使用INSERT IGNORE、取第一条或把重复数量随意相加来掩盖异常。

### 空值与索引一致性

store_id缺失、空字符串或仅空格统一按“0”参与键比较；MSKU/FNSKU缺失按空字符串参与比较，大小写、非空空格和Unicode原样保留。只用于键比较，不改原列及raw_json。Python与数据库使用相同语义。

数据库新增bin_identity_key VARBINARY(1600) STORED GENERATED，由三字段的“字符数:内容”顺序拼接，避免普通分隔符拼接碰撞、NULL绕过唯一约束和大小写不敏感排序规则误合并。它不是哈希，没有哈希碰撞问题，完整内容参与索引；应用INSERT不传该列，由MySQL自动计算。

索引变为(snapshot_date,sync_batch_id,wid,whb_id,product_id,bin_identity_key)。仓位表列数由22变23，其余4张表不变。导出列数及业务金额公式不变。

### 执行与回滚边界

1. 暂停周报触发，确认无RUNNING任务；备份date-project仓位ODS表及结构。不要清空旧快照或旧文件。
2. 用支持DELIMITER的MySQL客户端执行部署目录10_周报仓位业务键修复.sql。目标date-project，不是jmh_data_platform。需要ALTER/INDEX及临时存储过程权限；不要用不支持DELIMITER的旧Python分句执行器运行。
3. 脚本仅在缺列/旧索引时做一次ALTER，增加生成列并替换索引，不修改原字段、不DELETE、不DROP TABLE。获取inventory:weekly-export锁，任务运行中无法取得锁时显式失败。DDL会隐式提交并可能重建表；锁等待限制15秒，必须安排维护窗口，不能当作可由ROLLBACK撤回的事务。
4. 检查migration_status=OK、rows_before=rows_after，再执行11_周报仓位业务键_验证_只读.sql；索引检查OK、生成列定义正确、完整业务键重复查询0行。迁移对未知旧索引/同名异常列拒绝覆盖。已有合法新版结构再次执行跳过ALTER；2026-09-10已在本地MySQL 9.7连续执行两次通过。部署机仍需独立备份和验证，不能以本地结果替代。
5. 更新Python代码后重新加载，再手动执行一次周报验收。无需改Java/前端，无需权限SQL。新代码在拉数前检查生成列和完整唯一索引；漏迁移会立即提示执行10，不再白拉十几分钟。
6. 首次部署先02/04，再10/11和03。仓库初始化schema.sql已采用新版结构。02的旧仓位索引升级分支已加保护，重跑最新版02不会降级新索引；不要再运行缓存的旧版02。
7. 优先回滚应用并保留新列、新索引及历史行。新结构允许多个店铺明细，不可强行恢复旧三字段唯一键或为回滚而删除合法行。

仓库副本：
- Date-Project/migrations/20260910_weekly_bin_identity.sql
- Date-Project/migrations/20260910_weekly_bin_identity_verify.sql

### 本地执行记录（2026-09-10 10:09:07）

用户授权后已在本地 `date-project` 执行10迁移脚本两次（第二次验证幂等），然后执行11只读验证；部署机未执行。

- 执行前周报任务锁未被占用，最近两次周报均为failed；未修改任务启停状态。
- 已备份表结构及现有数据：`D:\JMH\项目\部署sql\backup_weekly_bin_20260910_100906_ilof7r0d\date-project_ods_lingxing_inventory_bin_detail_weekly_before.sql`，4582字节。该表执行前为空，备份因此仅含结构等SQL。
- 两次迁移均返回 `migration_status=OK, rows_before=0, rows_after=0`；未删除、合并或新增业务明细。
- 新增 `bin_identity_key VARBINARY(1600) STORED GENERATED`；`uk_bin_week` 更新为 `(snapshot_date,sync_batch_id,wid,whb_id,product_id,bin_identity_key)`。
- 11验证：`index_check=OK`、仓位表23列、完整业务键重复查询0行；Python `require_bin_identity_schema()` 通过。
- 本次仅升级仓位ODS结构，未操作 `jmh_data_platform`、未重启服务、未触发周报或其他同步。新整份周报的数据验收仍待重新运行任务；空表上的结构验证不能代替全链路验收。

部署时仍按本文件顺序执行10再11，不要导入上面的本地备份覆盖部署机数据。

### 重启补充（2026-09-10）

本地重启发现旧初始化代码 `schema.split(";")` 会把生成列注释中的英文分号当成语句结束，导致1064启动失败。已改用 `backend/sql_statements.py` 中识别引号和注释的分句函数，保留注释及数据库结构不变；这不是新的SQL迁移，不需要重复改表。部署Python时需同时包含该文件和更新后的 `backend/database.py`。

82项SQL分句及周报相关离线测试通过。修复后本地Python已启动，8010监听进程PID为28004，`/api/health`返回 `ok=true`、数据库 `date-project`。Java及前端未重启，也未手动触发同步任务。10/11迁移脚本内容未变，部署机仍按前述步骤执行。
