# JMH 跨境电商数据平台：前端产品需求与开发文档

> 文档版本：V1.0  
> 盘点日期：2026-07-22  
> 前端工程：`RuoYi-Vue3-master`  
> 对应后端：`RuoYi-Vue-springboot3`  
> 文档用途：作为后续前端视觉现代化、交互统一和页面重构的功能基线；本阶段不修改接口、不改变业务逻辑。

## 1. 文档结论与使用原则

本项目是基于 RuoYi-Vue3 的跨境电商运营、关务、财务及系统管理平台。前端现有业务已经覆盖 eBay、Amazon、报关、库存、财务、同步日志和若依标准后台能力。后续重构应把它从“通用管理后台”升级为“跨境电商数据工作台”，但必须保持下列契约不变：

1. 保持所有接口 URL、HTTP Method、参数结构、返回值解析和文件下载行为不变。
2. 保持后端动态菜单 `/getRouters` 与权限字符控制，不在前端写死用户可见菜单。
3. 保持 `v-hasPermi`、角色权限、路由权限、按钮权限和数据权限行为不变。
4. 保持查询、分页、排序、筛选、批量选择、导入、导出、编辑、保存、任务轮询等功能完整。
5. 保持跨页面跳转参数、隐藏详情页、标签页缓存、锁屏、错误页和登录回跳行为不变。
6. 视觉重构可以调整组件层级、布局、颜色、图标、动效和响应式方案，但不可因“简化界面”删除低频功能。

### 1.1 页面是否存在的判定

项目采用“前端固定路由 + 后端动态菜单”混合模式：

- 固定路由由 `src/router/index.js` 声明，主要是登录、注册、首页、锁屏、错误页和若干隐藏详情页。
- 登录后业务菜单来自 `GET /getRouters`，不同账号、角色和数据库菜单配置看到的页面可能不同。
- `src/views` 中存在的页面不等于当前角色一定能看到；本文件同时记录“代码已实现页面”和“当前部署可能开放的菜单”。
- SQL 中已有清理“系统工具”、部分系统监控菜单和报表中心的脚本，因此源码保留页应继续纳入功能基线，但重构前需以目标环境 `/getRouters` 返回结果决定是否显示。

## 2. 技术架构基线

### 2.1 核心技术

| 类别 | 当前实现 |
| --- | --- |
| 框架 | Vue 3.5、Composition API |
| 构建工具 | Vite 6 |
| UI 组件库 | Element Plus 2.13 |
| 路由 | Vue Router 4，固定路由与后端动态路由并存 |
| 状态管理 | Pinia 3 |
| HTTP | Axios 1.13 |
| 图表 | ECharts 5.6 |
| 富文本 | Quill |
| 文件处理 | file-saver、前端 Blob 下载、Excel multipart 上传 |
| 常用交互 | VueUse、vuedraggable、vue-cropper、Fuse.js |

### 2.2 请求通用规则

- 接口根地址：`VITE_APP_BASE_API`。
- 登录后请求头自动附加 `Authorization: Bearer <token>`。
- 默认超时 10 秒；大数据刷新、导入、同步和校准接口单独延长超时。
- GET 参数统一序列化；POST/PUT 默认启用短时间重复提交拦截。
- 业务成功码为 `200`；`401` 触发重新登录；`500` 显示错误；`601` 显示警告。
- Blob 响应直接交给文件下载流程，不走普通 JSON 提示。
- 若依通用导出使用 `POST application/x-www-form-urlencoded`；业务大表导出使用 Blob。

### 2.3 全局状态与持久化

| Store/机制 | 职责 |
| --- | --- |
| user | Token、用户 ID、用户名、昵称、头像、角色、权限；登录、用户信息、默认/过期密码提示、退出 |
| permission | 获取 `/getRouters`，将后端组件路径转换成可用 Vue 路由并按权限过滤 |
| app | 侧栏展开状态、设备类型、Element Plus 尺寸；Cookie 持久化 |
| settings | 主题色、暗色模式、导航布局、标签页、固定头部、Logo、标题、页脚 |
| tagsView | 已访问页、缓存页、iframe 页；关闭左侧/右侧/其他/全部标签，可选持久化 |
| 字典 | 页面通过字典类型取得状态、性别、菜单类型等显示标签 |
| 用户列配置 | `/system/user-column-config` 保存页面列显示和顺序，本地存储作为降级方案 |
| 任务轮询 | 同步/财务任务提交后约每 3 秒查询状态，直至成功、失败或结束 |

## 3. 全局框架与公共功能

### 3.1 登录后主框架

主框架由侧边导航、顶部导航、面包屑、标签页、内容区、设置面板和可选页脚组成。

全局操作包括：

- 展开/收起侧栏，移动端抽屉式导航。
- 面包屑导航、标签页切换、刷新当前页。
- 关闭当前/左侧/右侧/其他/全部标签页。
- 顶部菜单搜索、全屏、界面尺寸切换。
- 个人中心、布局设置、锁定屏幕、退出登录。
- 主题色、暗色模式、侧栏/顶部/混合导航、固定头部、显示 Logo、动态标题和页脚设置。
- 权限不足进入 401；路径不存在进入 404；重定向页处理标签刷新。

### 3.2 全局公共组件

| 组件 | 用途 |
| --- | --- |
| Pagination | 标准分页、页码、每页条数和总数 |
| RightToolbar | 查询区展开、刷新、列显隐等列表工具 |
| ColumnConfigDrawer | 业务大表列显示、顺序、固定列配置 |
| HeaderWithFilter / HeaderSearch | 表头筛选与关键字搜索 |
| DictTag | 字典值转文本和彩色状态标签 |
| FileUpload / ImageUpload / ImagePreview | 通用文件、图片上传和预览 |
| ExcelImportDialog | Excel 导入、上传状态和结果反馈 |
| Editor | 富文本公告内容编辑 |
| IconSelect / SvgIcon | 菜单图标选择和 SVG 图标显示 |
| TreePanel | 组织、菜单等树形筛选 |
| Crontab | Cron 表达式可视化编辑和执行时间预览 |
| Breadcrumb / Hamburger / TagsView | 主框架导航交互 |
| Screenfull / SizeSelect / HeaderSearch | 顶部工具条能力 |
| iFrame / ParentView | 动态路由 iframe 与目录容器 |

## 4. 路由与页面信息架构

### 4.1 固定路由

| 路径 | 页面 | 可见性/说明 |
| --- | --- | --- |
| `/login` | 登录页 | 白名单、未登录入口 |
| `/register` | 注册页 | 路由存在；当前登录页配置通常不显示注册链接 |
| `/index` | 首页 | 登录后默认首页、固定标签 |
| `/lock` | 锁屏页 | 已登录用户锁屏与解锁 |
| `/user/profile/:activeTab?` | 个人中心 | 隐藏路由，从头像菜单进入 |
| `/system/user-auth/role/:userId` | 分配用户角色 | 隐藏路由，要求 `system:user:edit` |
| `/system/role-auth/user/:roleId` | 分配角色用户 | 隐藏路由，要求 `system:role:edit` |
| `/system/dict-data/index/:dictId` | 字典数据 | 隐藏路由，要求 `system:dict:list` |
| `/monitor/job-log/index/:jobId` | 调度日志 | 隐藏路由，要求 `monitor:job:list` |
| `/redirect/:path(.*)` | 页面重定向 | 标签刷新用 |
| `/401` | 无权限页 | 权限不足 |
| `/:pathMatch(.*)*` | 404 页 | 未匹配路由 |

### 4.2 业务动态菜单

下列路径来自当前工程菜单结构和部署 SQL；最终路径以前端运行环境 `/getRouters` 返回为准。

| 一级领域 | 页面 | 典型路径 | 页面组件 |
| --- | --- | --- | --- |
| eBay 运营 | eBay 补货管理 | `/operations/ebay/ebay-replenishment` | `operations/ebay/replenishment/index` |
| eBay 运营 | eBay 价格追踪 | `/operations/ebay/ebay-price-tracking` | `operations/ebay/priceTracking/index` |
| eBay 运营 | eBay 补货公式 | `/operations/ebay/formula` | `operations/ebay/formula/index` |
| eBay 运营 | 品牌负责人 | `/operations/ebay/brand-owner` | `operations/brandOwner/index` |
| eBay 运营 | 链接模板 | `/operations/ebay/link-template` | `operations/linkTemplate/index` |
| Amazon 运营 | AMZ 补货管理 | `/operations/amz/amz-replenishment` | `operations/amz/replenishment/index` |
| Amazon 运营 | FBA 货件 | `/operations/amz/fba-shipment` | `operations/amz/fbaShipment/index` |
| Amazon 运营 | 补货公式配置 | `/operations/amz/formula-config` | `operations/amz/formulaConfig/index` |
| 关务 | 报关单 | `/operations/customs/declaration` | `operations/customs/declaration/index` |
| 关务 | 报关库存 | `/operations/customs/inventory` | `operations/customs/inventory/index` |
| 数据同步 | 同步日志 | `/operations/sync/sync-log` | `operations/sync/log/index` |
| 财务 | 外汇退税 | `/finance/export-tax-refund` | `finance/exportTaxRefund/index` |
| 财务 | eBay 财务 | `/finance/ebay-finance` | `finance/ebayFinance/index` |

### 4.3 若依系统与监控菜单

| 领域 | 页面 | 典型路径 |
| --- | --- | --- |
| 系统管理 | 用户管理 | `/system/user` |
| 系统管理 | 角色管理 | `/system/role` |
| 系统管理 | 菜单管理 | `/system/menu` |
| 系统管理 | 部门管理 | `/system/dept` |
| 系统管理 | 岗位管理 | `/system/post` |
| 系统管理 | 字典管理 | `/system/dict` |
| 系统管理 | 参数配置 | `/system/config` |
| 系统管理 | 通知公告 | `/system/notice` |
| 日志管理 | 操作日志 | `/system/log/operlog` |
| 日志管理 | 登录日志 | `/system/log/logininfor` |
| 系统监控 | 在线用户 | `/monitor/online` |
| 系统监控 | 定时任务 | `/monitor/job` |
| 系统监控 | Druid 数据监控 | `/monitor/druid` |
| 系统监控 | 服务监控 | `/monitor/server` |
| 系统监控 | 缓存监控 | `/monitor/cache` |
| 系统监控 | 缓存列表 | `/monitor/cacheList` |

这些路径同样由后端动态菜单控制；如果数据库菜单被清理，源码页仍在但不会向当前角色展示。

## 5. 认证与公共页面需求

### 5.1 登录页 `views/login.vue`

**目标**：完成账号认证并进入原访问页或首页。

**字段与显示**：账号、密码、验证码、记住密码；验证码由配置决定是否显示；支持系统标题和底部版权信息。

**操作与规则**：

- 校验账号和密码必填，验证码开启时校验验证码。
- 获取并刷新验证码图片：`GET /captchaImage`。
- 登录：`POST /login`，提交用户名、密码、验证码和验证码 UUID。
- “记住密码”在本地保存加密后的账号/密码和勾选状态。
- 登录成功后按 `redirect` 参数回跳，无参数进入 `/index`。
- 登录后通过 `GET /getInfo` 获取用户、角色、权限，再通过 `GET /getRouters` 生成菜单。
- 注册入口代码存在，但是否显示由本地 `register` 开关控制。

**现代化时不可遗漏**：验证码切换、回车提交、加载态、错误提示、记住密码、回跳、移动端适配、弱网状态。

### 5.2 注册页 `views/register.vue`

- 字段：用户名、密码、确认密码、验证码。
- 规则：长度、密码一致性、验证码必填。
- 操作：刷新验证码；`POST /register`；注册成功返回登录页。
- 当前是否向用户开放由系统配置和登录页入口决定。

### 5.3 锁屏页 `views/lock.vue`

- 显示当前用户头像和昵称。
- 输入当前账号密码调用 `POST /unlockscreen` 解锁。
- 可退出锁屏状态并返回登录流程。
- 锁屏期间访问业务路径强制跳转 `/lock`。

### 5.4 首页 `views/index.vue`

当前页面基本为空，是本次现代化的主要产品机会。后续可改造成跨境电商总览驾驶舱，但新指标必须先复用现有接口或另行立项，不能在纯样式改造中虚构数据接口。

建议在不增加接口时优先提供：常用模块快捷入口、最近访问页、当前用户欢迎信息、系统公告摘要、任务快捷入口和空状态引导。

### 5.5 错误与重定向页

- 401：提示无访问权限，提供返回首页或返回上一页。
- 404：提示页面不存在，提供回到首页。
- Redirect：用于刷新标签页并保持原目标路径。

## 6. eBay 运营页面

### 6.1 eBay 补货管理

**页面目标**：汇总多站点库存、销量、采购和补货指标，支持补货决策与数据同步。

**查询条件**：站点、SKU、产品名称、产品等级、产品性质、退货等级、负责人；支持表头独立筛选。

**核心数据**：站点、SKU、产品、等级、性质、预测值、利润率、退货率、海外在途/可售/总库存、采购未到、成都可售/在途、采购计划、锁定库存、总库存、7/15/30/90 天销量、月最大销量、库存销售比、本地最近出库、出库天数、采购周期、建议采购量、月最大补货量、退货等级、周转、负责人。

**操作**：

- 查询、重置、分页、排序、行选择、表头筛选。
- 拉取 eBay 最新数据；按钮权限 `operations:ebayReplenishment:sync`。
- Excel 导入下拉：销量 `import-sales`、利润率 `import-profit-rate`、退货率 `import-return-rate`；权限 `operations:ebayReplenishment:import`。
- 导出当前条件/列选择数据；权限 `operations:ebayReplenishment:export`。
- 列显隐、列顺序和固定列配置，并保存用户列配置。
- 统计/汇总行展示。

**接口**：

- `GET /operations/ebay/replenishment/list`
- `POST /operations/ebay/replenishment/search`
- `POST /operations/ebay/replenishment/refresh`
- `POST /operations/ebay/replenishment/import-sales`
- `POST /operations/ebay/replenishment/import-profit-rate`
- `POST /operations/ebay/replenishment/import-return-rate`
- `POST /operations/ebay/replenishment/export`

### 6.2 eBay 价格追踪

**页面目标**：集中管理站点 SKU 的最低价、追踪价、利润底线、链接和运营备注。

**查询条件**：站点、SKU、产品名称、品牌、产品等级、运营负责人；表头字段支持远程枚举/关键词筛选。

**核心数据**：站点、SKU、产品、等级、最低价、追踪价、追踪利润、底价、退货率、3/7/30/90 天销量、月最大销量、OE 号、预售/在售链接、海外库存、库龄、库存比、预估数量、品牌、负责人、备注、计算时间。

**操作**：

- 查询、重置、分页、排序、列筛选、列配置。
- 导入最低价 `import-lowest-price`、产品价格 `import-product-price`；沿用权限 `operations:ebayReplenishment:import`。
- 导出；沿用权限 `operations:ebayReplenishment:export`。
- 编辑追踪价：先计算利润/底价并保存追踪价格。
- 单元格编辑并保存 OE 号、备注。
- 刷新/重新计算追踪数据。

**接口**：

- `POST /operations/ebay/price-tracking/search`
- `GET /operations/ebay/price-tracking/distinct-values`
- `POST /operations/ebay/price-tracking/refresh`
- `POST /operations/ebay/price-tracking/calc-tracking`
- `POST /operations/ebay/price-tracking/save-tracking-price`
- `POST /operations/ebay/price-tracking/oe`
- `POST /operations/ebay/price-tracking/remark`
- `POST /operations/ebay/price-tracking/import-lowest-price`
- `POST /operations/ebay/price-tracking/import-product-price`
- `POST /operations/ebay/price-tracking/export`

### 6.3 eBay 补货公式

**页面目标**：维护补货计算策略及各周期销量权重。

**字段**：序号、分组、产品性质、公式、适用条件、基数、下限、上限、7/15/30 天权重、倍数、乘 30 参数、备注、状态。

**操作**：刷新列表；表格内编辑；单行保存；状态和参数反馈。

**接口**：

- `GET /operations/ebay/replenishment/formula/list`
- `POST /operations/ebay/replenishment/formula/update`

### 6.4 品牌负责人

**查询条件**：品牌编码、负责人。

**操作**：查询、重置、新增、编辑、删除；分别受 `operations:brandOwner:add/edit/remove` 控制。

**接口**：

- `GET /operations/brand-owner/list`
- `POST /operations/brand-owner`
- `PUT /operations/brand-owner`
- `DELETE /operations/brand-owner/{id}`

### 6.5 链接模板

**页面目标**：按站点维护预售/在售链接模板及定价辅助参数。

**字段**：站点、预售链接、在售链接、目标利润率、汇率等模板信息。

**操作**：加载列表、新增/保存、编辑、按站点删除。

**接口**：

- `GET /operations/ebay/price-tracking/link-template`
- `POST /operations/ebay/price-tracking/link-template`
- `DELETE /operations/ebay/price-tracking/link-template/{site}`

## 7. Amazon 运营页面

### 7.1 AMZ 补货管理

**页面目标**：按站点、店铺、仓库和商品维度完成库存、销售与建议补货分析。

**查询条件**：国家代码、店铺国家、店铺、MSKU、仓库、仓库 SKU、ASIN、负责人、产品分类、产品性质；区域快捷分组为全部/美国/欧洲；店铺支持全选、反选、取消、排除。

**核心数据**：站点/国家、店铺、MSKU、仓库 SKU、ASIN、商品、负责人、分类、性质、仓库、FBA 可售/在途/总量、国内可售/在途、采购未到、锁定与总库存、多周期销量、销售分解、销售趋势、库存销售比、补货周期、建议补货、已采购数量、备注等。

**操作**：

- 查询、重置、区域切换、分页、排序、表头筛选和汇总。
- 拉取 AMZ 最新数据；权限 `operations:amzReplenishment:sync`。
- 导出；权限 `operations:amzReplenishment:export`。
- 查看销量分解明细。
- 行内编辑产品分类、已采购数量和备注。
- 列显示、顺序和固定列个性化配置。

**接口**：

- `GET /operations/amz/replenishment/list`
- `POST /operations/amz/replenishment/search`
- `POST /operations/amz/replenishment/refresh`
- `POST /operations/amz/replenishment/export`
- `POST /operations/amz/replenishment/sales-breakdown/search`
- `GET /operations/amz/replenishment/store-names`
- `GET /operations/amz/replenishment/warehouse-names`
- `POST /operations/amz/replenishment/override`
- `POST /operations/amz/replenishment/update-qty-receive`

### 7.2 FBA 货件

**查询条件**：店铺、Shipment ID、SKU、MSKU、是否完成。

**核心数据**：店铺、货件 ID、货件名称、状态、MSKU、SKU、申报数量、接收数量、差异、创建人、创建/更新时间、各状态计数、备注、完成标记。

**操作**：查询、重置、分页、导出；行内保存备注；确认货件商品完成。导出沿用 `operations:amzReplenishment:export`。

**接口**：

- `POST /operations/amz/fba-shipment/search`
- `POST /operations/amz/fba-shipment/mark/remark`
- `POST /operations/amz/fba-shipment/mark/confirm`
- `GET /operations/amz/fba-shipment/store-names`
- `POST /operations/amz/fba-shipment/export`

### 7.3 AMZ 补货公式配置

**页面目标**：按区域设置仓库范围、销量权重、计算天数和策略开关。

**列表/表单字段**：区域分组、配置名称、启用状态、匹配仓库、销量权重、计算天数、备注等。

**操作**：加载配置、新增、查看详情、编辑保存、选择仓库、应用公式策略；新增按钮当前使用 `operations:amzReplenishment:list` 权限。

**接口**：

- `GET /operations/amz/formula-config/list`
- `GET /operations/amz/formula-config/{id}`
- `POST /operations/amz/formula-config/add`
- `PUT /operations/amz/formula-config/update`
- `GET /operations/amz/formula-config/warehouses`

## 8. 关务页面

### 8.1 报关单

**页面目标**：从 SKU、eBay 备货单、AMZ FBA 货件或历史报关单生成装箱和商品申报数据，并导出报关文件。

**报关头信息**：包含发货/申报主体、物流、目的国家/地区、仓库、发票、币种、贸易方式、运输方式、申报日期、订单/批次、箱数、总重、总价及备注等约 30 项字段，支持折叠显示。

**装箱与商品功能**：

- 管理包装组、箱号、长宽高、毛重/净重、件数和 SKU 数量。
- 根据 SKU 查询产品；展示中文/英文品名、申报要素、HS 编码、材质、用途、品牌、型号、单位、价格、原产国、税务/退税状态等。
- 支持区域和仓库数量分配、商品重复校验、已有产品信息检查。
- 保存商品资料；存在记录时支持覆盖确认。权限 `customs:product:edit`。

**数据来源与导入**：

- 关联 eBay Stock Order：搜索订单并加载所选订单商品。
- 关联 AMZ FBA Shipment：搜索货件并加载所选货件商品。
- Excel 导入 SKU。
- 导入单个/多个历史报关单。
- 导入 FBA 货件箱信息。

**主要操作**：清空/重置、关联数据源、保存商品、导出报关单。关联权限 `customs:declaration:query`，导出权限 `customs:declaration:export`；导入权限由菜单按钮配置继续保留。

**接口**：

- `GET /operations/customs/declaration/products/search`
- `GET /operations/customs/declaration/stock-orders/search`
- `POST /operations/customs/declaration/stock-orders/products`
- `GET /operations/customs/declaration/fba-shipments/search`
- `POST /operations/customs/declaration/fba-shipments/products`
- `POST /operations/customs/declaration/import-skus`
- `POST /operations/customs/declaration/import-history`
- `POST /operations/customs/declaration/import-histories`
- `POST /operations/customs/declaration/import-fba-shipment-box`
- `POST /operations/customs/declaration/products/existing`
- `PUT /operations/customs/declaration/products?overwrite=...`
- `POST /operations/customs/declaration/export`

### 8.2 报关库存

**页面目标**：维护可用于报关的产品主数据和库存属性。

**查询与数据**：关键字查询；商品编码/名称联想选择；字段涵盖中英文品名、HS 编码、品牌、型号、材质、用途、申报要素、单位、价格、币种、原产国、税率/退税率、重量、尺寸、箱规、状态、备注等。可编辑字段由后端动态返回，不能在重构中写死。

**操作**：

- 查询、重置、分页、勾选行。
- 新增，权限 `customs:inventory:add`。
- 编辑，权限 `customs:inventory:edit`。
- Excel 导入并显示导入反馈。
- 导出选中项或全部查询结果。
- 刷新列表，加载产品选项和动态可编辑字段。

**接口**：

- `GET /operations/customs/inventory/list`
- `POST /operations/customs/inventory/import`
- `GET /operations/customs/inventory/product-options`
- `GET /operations/customs/inventory/editable-fields`
- `POST /operations/customs/inventory`
- `PUT /operations/customs/inventory`
- `POST /operations/customs/inventory/export`

## 9. 财务页面

### 9.1 外汇退税

**页面结构**：四个业务页签——出口明细、采购库存、外汇应收、任务记录；另有导入、生成退税包和任务详情弹窗。

**出口明细**：按报关/发票/日期/主体等条件查询，可选择行或全量生成退税包；查看商品、数量、金额、币种、报关和匹配状态等。

**采购库存**：查询采购发票与库存匹配数据，展示采购方、供应商、发票、商品、数量、金额、税额和可用量等。

**外汇应收**：查询回款/应收数据，展示付款方、币种、金额、到账与匹配状态等。

**任务记录**：查看任务类型、状态、进度、提交人、开始/结束时间、结果文件和错误信息；打开任务详情；对运行任务进行轮询。

**导入与生成**：

- 导入报关材料、报关单、采购发票、外汇数据。
- 生成退税包时支持选中/全部、输出目录、付款方和覆盖开关。
- 查询权限 `finance:exportTaxRefund:query`；生成权限 `finance:exportTaxRefund:generate`；菜单 SQL 同时定义 `import`、`export` 权限，重构必须保留其入口与扩展位。

**接口**：

- `POST /finance/export-tax-refund/tasks/customs-material`
- `POST /finance/export-tax-refund/tasks/customs-declaration`
- `POST /finance/export-tax-refund/tasks/purchase-invoice`
- `POST /finance/export-tax-refund/tasks/forex`
- `POST /finance/export-tax-refund/tasks/refund-package`
- `GET /finance/export-tax-refund/tasks/{taskId}`
- `GET /finance/export-tax-refund/tasks`
- `GET /finance/export-tax-refund/customs-material-items`
- `GET /finance/export-tax-refund/export-details`
- `GET /finance/export-tax-refund/purchase-inventory`
- `GET /finance/export-tax-refund/forex-receivables`

### 9.2 eBay 财务

**查询条件**：站点、SKU、财务期间；字段标签可按数据动态显示。

**明细数据**：站点、开始/结束日期、SKU、订单金额、销量、订单数、利润、利润率、平台费用、采购成本、广告费、最后修改时间等。

**导入历史**：文件名、期间、有效行、插入数、覆盖数、导入人、导入时间、状态。

**操作**：查询、重置、刷新、分页；导入 Chief 利润数据；查看导入记录；打开编辑弹窗；保存/取消编辑。导入权限 `finance:ebayFinance:import`，编辑权限 `finance:ebayFinance:edit`。

**接口**：

- `GET /finance/ebay-finance/list`
- `GET /finance/ebay-finance/imports`
- `POST /finance/ebay-finance/import`
- `PUT /finance/ebay-finance/{id}`

## 10. 数据同步页面

### 10.1 同步日志

**查询条件**：同步类型、执行状态、触发方式等。

**列表**：父级同步任务、提交/开始/结束时间、耗时、成功/失败数量、执行人、消息；展开显示步骤/子任务记录。

**操作**：查询、重置、分页、展开步骤、打开详情弹窗并查看完整日志与错误信息。

**接口**：

- `GET /operations/sync/log/list`
- `GET /operations/sync/log/{id}`

### 10.2 全局同步能力

这些接口主要由业务页的“拉取最新数据”或同步入口调用：

- `POST /operations/sync/manual/ebay`
- `POST /operations/sync/manual/ebay/refresh-only`
- `POST /operations/sync/manual/amz`
- `POST /operations/sync/manual/amz/refresh-only`
- `POST /operations/sync/manual/stock-order`
- `GET /operations/sync/manual/status/{submissionId}`
- `POST /operations/sync/calibration/full`，最长任务超时 30 分钟。

所有异步同步必须显示提交成功、运行中、进度/状态、完成或失败反馈，禁止只保留一个无状态按钮。

## 11. 系统管理页面

### 11.1 用户管理

**结构**：左侧部门树；右侧查询区、工具栏、用户表格；新增/编辑、导入、重置密码等弹窗。

**查询条件**：用户名、手机号、状态、创建时间范围；部门树筛选。

**数据字段**：用户编号、用户名、昵称、部门、手机号、状态、创建时间等。

**操作与权限**：

- 新增 `system:user:add`；修改 `system:user:edit`；删除 `system:user:remove`。
- 导入 `system:user:import`：下载模板、选择是否覆盖、上传 Excel、显示导入结果。
- 导出 `system:user:export`。
- 启用/停用用户；重置密码 `system:user:resetPwd`。
- 分配角色；进入隐藏路由 `/system/user-auth/role/{userId}`。
- 查看用户详情；分页、多选、查询、重置。

**接口**：`/system/user/list`、`/system/user/{id}`、`POST/PUT /system/user`、`DELETE /system/user/{id}`、`PUT /system/user/resetPwd`、`PUT /system/user/changeStatus`、`GET/PUT /system/user/authRole`、`GET /system/user/deptTree`、`POST /system/user/importData`、`POST /system/user/importTemplate`、`POST system/user/export`。

### 11.2 用户详情与个人中心

**用户详情 `system/user/view.vue`**：显示用户基础资料、部门、岗位、角色、状态和创建信息，并支持返回列表。

**个人中心**：

- 资料总览：头像、用户名、手机号、邮箱、部门、角色、创建时间。
- 基本资料：修改昵称、手机号、邮箱、性别等，`GET/PUT /system/user/profile`。
- 修改密码：旧密码、新密码、确认密码，`PUT /system/user/profile/updatePwd`。
- 修改头像：裁剪、缩放、上传，`POST /system/user/profile/avatar`。

### 11.3 用户分配角色

- 展示当前用户基础信息和角色列表。
- 勾选角色并提交授权；支持返回用户列表。
- 接口：`GET /system/user/authRole/{userId}`、`PUT /system/user/authRole`。
- 路由权限：`system:user:edit`。

### 11.4 角色管理

**查询条件**：角色名称、权限字符、状态、创建时间。

**操作**：新增、修改、删除、导出、启停；菜单权限树；数据权限范围设置；分配用户。权限字符分别为 `system:role:add/edit/remove/export`。

**接口**：`/system/role/list`、`/system/role/{id}`、`POST/PUT /system/role`、`DELETE /system/role/{id}`、`PUT /system/role/changeStatus`、`PUT /system/role/dataScope`、`GET /system/role/deptTree/{roleId}`、`POST system/role/export`。

### 11.5 角色分配用户

- 已分配用户：按用户名、手机号查询，批量/单个取消授权。
- 选择用户弹窗：查询未分配用户，多选后批量授权。
- 接口：`GET /system/role/authUser/allocatedList`、`GET /unallocatedList`、`PUT /cancel`、`PUT /cancelAll`、`PUT /selectAll`。
- 主要权限：增加授权 `system:role:add`，取消授权 `system:role:remove`。

### 11.6 菜单管理

**数据**：菜单名称、图标、排序、权限标识、组件路径、状态、创建时间，树形展示并支持展开/折叠。

**表单**：目录/菜单/按钮类型，父菜单、图标、路由地址、组件路径、路由参数、路由名称、权限字符、显示排序、外链、缓存、显示、状态。

**操作**：查询、重置、新增顶级/子级、编辑、删除、拖拽/调整排序；权限 `system:menu:add/edit/remove`。

**接口**：`GET /system/menu/list`、`GET /system/menu/{id}`、`GET /system/menu/treeselect`、`GET /system/menu/roleMenuTreeselect/{roleId}`、`POST/PUT /system/menu`、`PUT /system/menu/updateSort`、`DELETE /system/menu/{id}`。

### 11.7 部门管理

**数据/表单**：部门树、上级部门、名称、负责人、电话、邮箱、排序、状态。

**操作**：查询、展开/折叠、新增、修改、删除、调整排序；权限 `system:dept:add/edit/remove`。

**接口**：`GET /system/dept/list`、`GET /system/dept/list/exclude/{id}`、`GET /system/dept/{id}`、`POST/PUT /system/dept`、`PUT /system/dept/updateSort`、`DELETE /system/dept/{id}`。

### 11.8 岗位管理

**查询条件**：岗位编码、岗位名称、状态。

**字段**：岗位编号、编码、名称、排序、状态、创建时间、备注。

**操作**：新增、修改、删除、导出、分页；权限 `system:post:add/edit/remove/export`。

**接口**：`GET /system/post/list`、`GET /system/post/{id}`、`POST/PUT /system/post`、`DELETE /system/post/{id}`、`POST system/post/export`。

### 11.9 字典类型

**查询条件**：字典名称、字典类型、状态、创建时间。

**操作**：新增、修改、删除、导出、刷新缓存、进入字典数据列表；权限 `system:dict:add/edit/remove/export`。

**接口**：`GET /system/dict/type/list`、`GET /system/dict/type/{id}`、`POST/PUT /system/dict/type`、`DELETE /system/dict/type/{id}`、`DELETE /system/dict/type/refreshCache`、`GET /system/dict/type/optionselect`、`POST system/dict/type/export`。

### 11.10 字典数据

**查询条件**：字典标签、状态；由字典类型 ID/类型进入。

**字段**：编码、排序、标签、键值、样式属性、回显样式、状态、备注。

**操作**：新增、修改、删除、导出、返回字典类型；权限复用 `system:dict:add/edit/remove/export`。

**接口**：`GET /system/dict/data/list`、`GET /system/dict/data/{code}`、`GET /system/dict/data/type/{type}`、`POST/PUT /system/dict/data`、`DELETE /system/dict/data/{code}`、`POST system/dict/data/export`。

`system/dict/detail.vue` 为字典详情/兼容视图源码，也应随字典页面样式体系一起维护。

### 11.11 参数配置

**查询条件**：参数名称、参数键名、系统内置、创建时间。

**字段**：参数编号、名称、键名、键值、系统内置、备注、创建时间。

**操作**：新增、修改、删除、导出、刷新缓存、分页；权限 `system:config:add/edit/remove/export`。

**接口**：`GET /system/config/list`、`GET /system/config/{id}`、`GET /system/config/configKey/{key}`、`POST/PUT /system/config`、`DELETE /system/config/{id}`、`DELETE /system/config/refreshCache`、`POST system/config/export`。

### 11.12 通知公告

**查询条件**：公告标题、操作人员、公告类型。

**字段**：编号、标题、类型、状态、创建者、创建时间；公告内容为富文本。

**操作**：新增、修改、删除、查看；查看阅读用户；顶部公告列表；标记单条已读、全部已读。权限 `system:notice:add/edit/remove/list`。

**接口**：`GET /system/notice/list`、`GET /system/notice/{id}`、`POST/PUT /system/notice`、`DELETE /system/notice/{id}`、`GET /system/notice/listTop`、`POST /system/notice/markRead`、`POST /system/notice/markReadAll`、`GET /system/notice/readUsers/list`。

## 12. 日志与系统监控页面

### 12.1 操作日志

**查询条件**：系统模块、操作人员、操作类型、操作状态、操作时间。

**字段**：日志编号、模块、操作类型、请求方式、操作人员、地址、地点、状态、耗时、操作时间。

**操作**：查询、重置、删除、清空、导出、查看详情；权限 `monitor:operlog:remove/export/query`。

**详情**：操作模块、登录信息、请求地址、方法、参数、返回结果、异常信息、耗时。

**接口**：`GET /monitor/operlog/list`、`DELETE /monitor/operlog/{id}`、`DELETE /monitor/operlog/clean`、`POST monitor/operlog/export`。

### 12.2 登录日志

**查询条件**：登录地址、用户名、状态、登录时间。

**字段**：访问编号、用户名、地址、地点、浏览器、操作系统、登录状态、提示消息、访问时间。

**操作**：删除、清空、解锁账号、导出；权限 `monitor:logininfor:remove/unlock/export`。

**接口**：`GET /monitor/logininfor/list`、`DELETE /monitor/logininfor/{id}`、`GET /monitor/logininfor/unlock/{userName}`、`DELETE /monitor/logininfor/clean`、`POST monitor/logininfor/export`。

### 12.3 在线用户

**查询条件**：登录地址、用户名。

**字段**：会话编号、用户名、部门、主机、登录地点、浏览器、操作系统、登录时间。

**操作**：查询、重置、强制退出；权限 `monitor:online:forceLogout`。

**接口**：`GET /monitor/online/list`、`DELETE /monitor/online/{tokenId}`。

### 12.4 定时任务

**查询条件**：任务名称、任务组、状态。

**字段**：编号、名称、组名、调用目标、Cron 表达式、状态。

**操作**：新增、修改、删除、启停、立即执行、查看详情、查看调度日志、导出；Cron 可视化编辑和未来执行时间预览。权限 `monitor:job:add/edit/remove/export/query/changeStatus`。

**接口**：`GET /monitor/job/list`、`GET /monitor/job/{id}`、`POST/PUT /monitor/job`、`DELETE /monitor/job/{id}`、`PUT /monitor/job/changeStatus`、`PUT /monitor/job/run`、`POST monitor/job/export`。

### 12.5 调度日志

**查询条件**：任务名称、任务组、执行状态、执行时间；支持按 `jobId` 进入。

**字段**：日志编号、任务名称、任务组、调用目标、日志信息、执行状态、执行时间、异常信息。

**操作**：删除、清空、导出、查看详情；权限复用 `monitor:job:remove/export/query`。

**接口**：`GET /monitor/jobLog/list`、`DELETE /monitor/jobLog/{id}`、`DELETE /monitor/jobLog/clean`、`POST monitor/jobLog/export`。

### 12.6 服务监控

- 展示 CPU 核心与使用率、内存、JVM、服务器信息、Java/系统参数、磁盘容量和使用率。
- 自动/手动加载监控数据。
- 接口：`GET /monitor/server`。

### 12.7 缓存监控

- 展示 Redis 基本信息、命令统计、内存使用图表和缓存键数量。
- 接口：`GET /monitor/cache`。

### 12.8 缓存列表

- 左侧缓存名称，中部键列表，右侧键值详情。
- 操作：选择缓存、查询键、查看值、清除单个键、清除某缓存名称、清除全部缓存。
- 接口：`GET /monitor/cache/getNames`、`GET /getKeys/{cacheName}`、`GET /getValue/{cacheName}/{cacheKey}`、`DELETE /clearCacheName/{cacheName}`、`DELETE /clearCacheKey/{cacheKey}`、`DELETE /clearCacheAll`，均以 `/monitor/cache` 为前缀。

### 12.9 Druid 数据监控

- `monitor/druid/index.vue` 以 iframe/外链方式承载 Druid 监控页。
- 现代化主框架需要保证 iframe 高度、加载态、跨域失败提示和标签页刷新正常。

## 13. 权限基线

后续重构必须继续把按钮权限挂在真实交互元素上，不可只隐藏菜单但保留可点击入口。已在页面中明确使用的权限包括：

| 领域 | 权限字符 |
| --- | --- |
| eBay 补货/价格 | `operations:ebayReplenishment:sync/import/export` |
| AMZ 补货/FBA | `operations:amzReplenishment:list/sync/export` |
| 品牌负责人 | `operations:brandOwner:add/edit/remove` |
| 报关单 | `customs:declaration:query/export`、`customs:product:edit` |
| 报关库存 | `customs:inventory:add/edit` |
| 外汇退税 | `finance:exportTaxRefund:query/import/export/generate` |
| eBay 财务 | `finance:ebayFinance:list/import/edit` |
| 用户 | `system:user:add/edit/remove/import/export/resetPwd` |
| 角色 | `system:role:add/edit/remove/export` |
| 菜单 | `system:menu:add/edit/remove` |
| 部门 | `system:dept:add/edit/remove` |
| 岗位 | `system:post:add/edit/remove/export` |
| 字典 | `system:dict:add/edit/remove/export/list` |
| 参数 | `system:config:add/edit/remove/export` |
| 公告 | `system:notice:add/edit/remove/list` |
| 操作日志 | `monitor:operlog:query/remove/export` |
| 登录日志 | `monitor:logininfor:remove/unlock/export` |
| 在线用户 | `monitor:online:forceLogout` |
| 定时任务 | `monitor:job:add/edit/remove/export/query/changeStatus/list` |

说明：部分业务页面只在菜单层校验 `list` 权限，部分按钮直接复用相邻模块权限。视觉重构应原样保留，权限命名优化属于后端与数据库联合变更，不在纯前端样式范围内。

## 14. 前端现代化改造的产品验收标准

### 14.1 统一设计语言

- 建立跨境电商主题设计 Token：品牌色、成功/预警/危险/信息色、背景层级、边框、阴影、圆角、间距、字号、行高和动效时长。
- 业务颜色需有语义：利润/增长、亏损/异常、库存不足、库存积压、同步进行中、同步失败、完成。
- 暗色模式下表格、弹窗、图表、固定列和悬浮状态必须完整适配。
- 使用统一图标体系；不靠颜色单独表达状态。

### 14.2 主框架

- 侧栏按 eBay、Amazon、关务、财务、系统形成清晰分组，并继续由动态菜单驱动。
- 顶栏突出全局搜索、任务状态、公告、个人中心和主题设置。
- 标签页支持当前既有全部关闭行为，并提供明确的激活态和未保存状态预留。
- 首页改造为工作台时只能组合已有数据或快捷入口；新增经营指标需先定义接口。

### 14.3 列表与大数据表

- 查询区采用可折叠高级筛选；常用条件首屏可见。
- 主操作、批量操作、危险操作建立一致层级；禁用态说明原因。
- 保持固定列、横向滚动、列配置、表头筛选、排序、分页、汇总和勾选。
- 数字统一千分位、小数位、货币和百分比；空值统一为 `--`。
- 大表加载采用骨架/局部加载，避免全屏抖动；空状态、无结果、请求失败分开设计。
- 行内编辑必须有编辑态、校验、保存中、保存成功/失败和撤销反馈。

### 14.4 导入、导出与长任务

- 导入弹窗明确支持格式、模板、大小、覆盖规则、上传进度和成功/失败明细。
- 导出必须显示范围（当前筛选/选中/全部）、生成中和下载反馈。
- 同步、刷新、校准、退税包生成等长任务必须防重复点击，并保持轮询、进度和错误详情。
- 页面切换后如任务仍执行，应在全局任务入口保留可发现性；若不新增状态能力，至少保持当前页面内轮询。

### 14.5 动效

- 页面进入、筛选区展开、弹窗、抽屉、状态变化使用 150–300ms 的轻量动效。
- 表格数据刷新不使用大幅位移动画；数字变化可使用克制的过渡。
- 同步与生成任务使用进度动效；成功/失败只做一次反馈，避免无限闪烁。
- 遵循 `prefers-reduced-motion`，低性能设备减少非必要动效。

### 14.6 响应式与可访问性

- 1366、1440、1920 宽度下优先保证业务大表可用；移动端保留查询和关键操作，复杂表格可横向滚动。
- 所有表单具有可识别标签、错误文本和键盘焦点；弹窗支持 Esc 与焦点回收。
- 图标按钮提供 Tooltip/无障碍名称；状态标签具有文本。
- 颜色对比度、点击热区和键盘操作满足后台系统基本可访问性要求。

### 14.7 回归验收

每个页面至少验证：

1. 路由可进入，刷新不丢失，标签缓存符合原行为。
2. 无权限按钮不可见，无权限路由不可进入。
3. 查询、重置、分页、排序、筛选、勾选和列配置正常。
4. 新增、编辑、删除、启停、确认和危险操作二次确认正常。
5. 上传、导入、导出和 Blob 文件名处理正常。
6. 长任务不会重复提交，轮询能停止并正确显示失败原因。
7. 空数据、接口错误、401、超时和弱网有可理解反馈。
8. 明暗主题、主流桌面分辨率和移动端导航无明显错位。

## 15. 接口模块覆盖清单

本次共核对 `src/api` 下 30 个 JavaScript 文件：

```text
api/login.js
api/menu.js
api/finance/ebayFinance.js
api/finance/exportTaxRefund.js
api/monitor/cache.js
api/monitor/job.js
api/monitor/jobLog.js
api/monitor/logininfor.js
api/monitor/online.js
api/monitor/operlog.js
api/monitor/server.js
api/operations/amz/fbaShipment.js
api/operations/amz/formulaConfig.js
api/operations/amz/replenishment.js
api/operations/customs/declaration.js
api/operations/customs/inventory.js
api/operations/ebay/priceTracking.js
api/operations/ebay/replenishment.js
api/operations/sync.js
api/operations/syncLog.js
api/system/config.js
api/system/dept.js
api/system/dict/data.js
api/system/dict/type.js
api/system/menu.js
api/system/notice.js
api/system/post.js
api/system/role.js
api/system/user.js
api/system/userColumnConfig.js
```

另有品牌负责人、链接模板、eBay 公式、部分导入导出和 AMZ 行内编辑接口直接写在页面组件中，已分别记入对应页面章节，不能因不在 `src/api` 目录而遗漏。

## 16. Vue 页面覆盖清单

本次共核对 `src/views` 下 50 个 Vue 文件：

```text
views/login.vue
views/register.vue
views/index.vue
views/lock.vue
views/redirect/index.vue
views/error/401.vue
views/error/404.vue

views/finance/ebayFinance/index.vue
views/finance/exportTaxRefund/index.vue

views/operations/ebay/replenishment/index.vue
views/operations/ebay/priceTracking/index.vue
views/operations/ebay/formula/index.vue
views/operations/amz/replenishment/index.vue
views/operations/amz/fbaShipment/index.vue
views/operations/amz/formulaConfig/index.vue
views/operations/brandOwner/index.vue
views/operations/linkTemplate/index.vue
views/operations/customs/declaration/index.vue
views/operations/customs/inventory/index.vue
views/operations/sync/log/index.vue

views/system/user/index.vue
views/system/user/view.vue
views/system/user/authRole.vue
views/system/user/profile/index.vue
views/system/user/profile/userInfo.vue
views/system/user/profile/resetPwd.vue
views/system/user/profile/userAvatar.vue
views/system/role/index.vue
views/system/role/authUser.vue
views/system/role/selectUser.vue
views/system/menu/index.vue
views/system/dept/index.vue
views/system/post/index.vue
views/system/dict/index.vue
views/system/dict/data.vue
views/system/dict/detail.vue
views/system/config/index.vue
views/system/notice/index.vue
views/system/notice/ReadUsers.vue

views/monitor/online/index.vue
views/monitor/job/index.vue
views/monitor/job/detail.vue
views/monitor/job/log.vue
views/monitor/druid/index.vue
views/monitor/server/index.vue
views/monitor/cache/index.vue
views/monitor/cache/list.vue
views/monitor/operlog/index.vue
views/monitor/operlog/detail.vue
views/monitor/logininfor/index.vue

```

## 17. 已知边界与重构前确认项

1. 业务菜单由数据库和角色动态返回；正式设计清单应在目标账号下导出一次 `/getRouters` 响应，与本文件对照确认菜单名称、排序和隐藏状态。
2. 本次运行页复核中，新开的隔离浏览器标签未继承现有登录标签的会话，且路由初始化接口请求失败，因此没有以该隔离标签的空白状态推断页面缺失；功能清单以完整源码、路由、权限和接口为准。
3. 首页当前没有业务数据接口；若要做真实经营驾驶舱，需要单独定义指标、口径、权限、刷新频率和后端聚合接口。
4. 参数配置和部分监控页面在源码存在，但数据库清理脚本可能使它们不出现在当前角色菜单。先确认保留/隐藏，再决定视觉改造优先级。
5. 个别业务按钮复用相邻模块权限，个别直接请求未封装到 `src/api`。本次纯样式阶段应保持现状；后续可作为代码治理项目统一。

---

本文件是前端现代化的“功能不得丢失”基线。每完成一个页面重构，应使用第 14.7 节回归清单，并逐项对照本页的字段、按钮、弹窗、接口和权限字符。
